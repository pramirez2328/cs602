Run 
npm install 
in each of the folders before running the samples

1_Server (Apollo Server)
    (requires local monggodb instance)

  - node dbInit
  - node server_graphQL_apollo

Run the server, server_graphQL_apollo, before runing the clients below.

Clients:

2_Client

  - node graphQL_client

3_React 
  npm install
  npm start
